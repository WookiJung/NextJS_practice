
function createTimer (hh, mm, ss) {
  // const componentStyle = document.createElement("style")
  // componentStyle.innerHTML= "#component{position: absolute;z-index: 9;background-color: #f1f1f1;border: 1px solid #d3d3d3;text-align: center;}" 
  // componentStyle.innerHTML= "#componentheader{padding: 10px;cursor: move;z-index: 10;background-color: #2196F3;color: #fff;}"
  // document.head.appendChild(componentStyle)

  // const component = document.createElement('div')
  // component.id = "component"
  // const componentHeader = document.createElement("div")
  // componentHeader.id = "componentheader"
  // componentHeader.innerText = "여길클릭해서 이동"
  // const timeComponent = document.createElement('p')
  // timeComponent.innerText = `${hh}, ${mm}, ${ss}`
  // component.appendChild(componentHeader)
  // component.appendChild(timeComponent)
  // const body = document.querySelector('.page-header')
  // body?.appendChild(component)
}

    

export default createTimer